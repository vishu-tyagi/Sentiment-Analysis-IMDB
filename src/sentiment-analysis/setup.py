import setuptools

setuptools.setup(
    name="sentiment-analysis",
    version="0.0.1",
    description="Package for sentiment analysis",
    packages=setuptools.find_packages(),
    python_requires=">=3.9"
)
